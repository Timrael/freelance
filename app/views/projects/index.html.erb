<div class="hero-unit">
    <h1>Projects on competition</h1>
    <br>
    <p>
      Here you can find projects on competition for freelancers.
      Chose some, add bid with description and price, participate and win!
    </p>
  <p>
    Also you can create your own projects and put up them on bidding!
  </p>
    <p><%= link_to "Create a project", :new_project, class: "btn btn-primary btn-large" %></p>
</div>

<table class="table table-striped table-bordered">
  <tbody>
    <% projects.each do |project| %>
        <tr class="<%= "success" if project.have_chosen_bid? %>">
          <td>
              <h3><%= link_to project.title, action: :show, id: project.id %></h3>
              <p><%= project.description %></p>
              <p class="controls-row">
                <span class="span4">
                  <strong>Author: </strong><%= project.author.email %>
                </span>
                <span class="span3">
                  <strong>Published:</strong> <%= time_ago_in_words project.created_at %> ago
                </span>
                <span class="span3">
                    <strong>Condition:</strong> <%= project.condition %>
                </span>
                <span class="span3">
                  <strong>Bids:</strong> <%= link_to project.bids.count, action: :show, id: project.id %>
                </span>
              </p>
          </td>
        </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate projects %>
</div>
